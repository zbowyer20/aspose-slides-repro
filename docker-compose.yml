version: '3'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./pdfs:/pdfs
      - ./fonts:/fonts
      - ./fonts-external:/fonts-external
      - ./testFiles:/testFiles